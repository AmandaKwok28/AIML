File structure:

data-augmentation
|
├── data
|   └── canvas-data
|       └── Global Health +AI course fall 2025 (downlaoded this from the google drive link in canvas)
|
└── pipeline
    └── load-image.py             

In the above file structure, the load-image.py kind of expects that you're inside the pipeline folder to run.
You can change the file paths if you want. Just a convenient way to load the images as you're labelling.
